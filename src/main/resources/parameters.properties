oracle_driver oracle.jdbc.driver.OracleDriver
oracle_select_part1 SELECT DISTINCT TABLE_TO, FIELD_TO, TABLE_FROM, FIELD_FROM FROM (SELECT CONS.TABLE_NAME TABLE_TO, COLS.COLUMN_NAME FIELD_TO, CONS_R.TABLE_NAME TABLE_FROM, COLS_R.COLUMN_NAME FIELD_FROM FROM USER_CONSTRAINTS CONS LEFT JOIN USER_CONS_COLUMNS COLS ON COLS.CONSTRAINT_NAME = CONS.CONSTRAINT_NAME LEFT JOIN USER_CONSTRAINTS CONS_R ON CONS_R.CONSTRAINT_NAME = CONS.R_CONSTRAINT_NAME LEFT JOIN USER_CONS_COLUMNS COLS_R ON COLS_R.CONSTRAINT_NAME = CONS.R_CONSTRAINT_NAME WHERE CONS.TABLE_NAME IS NOT NULL AND COLS.COLUMN_NAME IS NOT NULL AND CONS_R.TABLE_NAME IS NOT NULL AND COLS_R.COLUMN_NAME IS NOT NULL AND CONS.CONSTRAINT_TYPE = 'R' and CONS.table_name in (
oracle_select_part2 ))